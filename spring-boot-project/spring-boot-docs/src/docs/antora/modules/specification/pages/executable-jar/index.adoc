[appendix]
[[appendix.executable-jar]]
= 可执行Jar格式

`spring-boot-loader` 模块让 Spring Boot 支持可执行的 jar 和 war 文件。
如果你使用 Maven 插件或 Gradle 插件，可执行 jar 会自动生成，通常无需关心其工作细节。

如果你需要通过其他构建系统创建可执行 jar，或只是想了解底层技术，本附录将提供相关背景。
