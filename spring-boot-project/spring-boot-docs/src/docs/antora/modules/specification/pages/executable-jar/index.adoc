[appendix]
[[appendix.executable-jar]]
= 可执行 Jar 格式

`spring-boot-loader` 模块让 Spring Boot 支持可执行的 jar 和 war 文件。
如果您使用 Maven 插件或 Gradle 插件，可执行 jar 会自动生成，您通常不需要了解它们的工作原理细节。

如果您需要从不同的构建系统创建可执行 jar，或者您只是对底层技术感到好奇，本附录提供了一些背景信息。
