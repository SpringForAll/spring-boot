[[build-tool-plugins.other-build-systems]]
= 支持其他构建系统

如果您想使用 Maven、Gradle 或 Ant 之外的构建工具，您可能需要开发自己的插件。
可执行 jar 需要遵循特定格式，并且某些条目需要以未压缩形式编写（有关详细信息，请参阅附录中的 xref:specification:/executable-jar/index.adoc[可执行 jar 格式] 部分）。

Spring Boot Maven 和 Gradle 插件都使用 `spring-boot-loader-tools` 实际生成 jar。
如果需要，您可以直接使用此库。



[[build-tool-plugins.other-build-systems.repackaging-archives]]
== 重新打包归档文件

要重新打包现有归档文件以使其成为自包含的可执行归档文件，请使用 javadoc:org.springframework.boot.loader.tools.Repackager[]。
javadoc:org.springframework.boot.loader.tools.Repackager[] 类接受一个指向现有 jar 或 war 归档文件的单一构造函数参数。
使用两种可用的 `repackage()` 方法之一来替换原始文件或写入新目标。
在运行 repackager 之前，还可以配置各种设置。



[[build-tool-plugins.other-build-systems.nested-libraries]]
== 嵌套库

在重新打包归档文件时，您可以使用 javadoc:org.springframework.boot.loader.tools.Libraries[] 接口包含对依赖文件的引用。
我们在此不提供 javadoc:org.springframework.boot.loader.tools.Libraries[] 的任何具体实现，因为它们通常是构建系统特定的。

如果您的归档文件已经包含库，您可以使用 javadoc:org.springframework.boot.loader.tools.Libraries#NONE[]。



[[build-tool-plugins.other-build-systems.finding-main-class]]
== 查找主类

如果您不使用 `Repackager.setMainClass()` 指定主类，repackager 将使用 https://asm.ow2.io/[ASM] 读取类文件并尝试找到具有 `public static void main(String[] args)` 方法的合适类。
如果找到多个候选项，则会抛出异常。



[[build-tool-plugins.other-build-systems.example-repackage-implementation]]
== 示例重新打包实现

以下示例显示了一个典型的重新打包实现：

include-code::MyBuildTool[]
