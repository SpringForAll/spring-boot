[appendix]
[[appendix.auto-configuration-classes]]
= 自动配置类

本附录包含 Spring Boot 提供的所有自动配置类的详细信息，包括指向文档和源代码的链接。
请记得查看应用程序中的条件报告，以获取更多关于哪些功能被启用的详细信息。
（为此，请使用 `--debug` 或 `-Ddebug` 启动应用程序，或者在 Actuator 应用程序中，使用 `conditions` 端点）。